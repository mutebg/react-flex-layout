import * as React from "react";
const {
  cloneElement,
  createContext,
  Component,
  createElement,
  forwardRef,
} = React;

export { cloneElement, createContext, Component, createElement, forwardRef };
